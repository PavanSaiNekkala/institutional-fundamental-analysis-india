version: "3.9"

services:

  institutional-dashboard:

    build: .

    container_name: institutional-dashboard

    ports:
      - "8501:8501"

    restart: always

    volumes:
      - .:/app

    environment:
      - PYTHONUNBUFFERED=1
